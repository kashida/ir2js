::
  @str\s\$

:create = ##
  \A<%.Implements>\
  context\context.Context\$
  line\input.Line\$
  str\s\$
  if !str
    => []
  impls := &type.ImplementsParser(context, line, str).parse()
  l(impls, 'impls')
  => impls.map(##)
    impl\s\$
    => &%.Implements(impl)

:ctorOutput\s\
  => '@implements {' + @str + '}'

:ifaceOutput\s\
  => '@extends {' + @str + '}'
