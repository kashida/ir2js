::^%.Code
  ^()

:create = ##
  \%.Native|-\
  scope\FileScope\$
  line\s\$
  => line == '~' ? &%.Native() : null

:output\A<output.Line>\
  => @.lines.map(##)
    line\input.Line\$
    out := &output.Line(line)
    out.appendLine(line.trim)
    => out
